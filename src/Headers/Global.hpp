#pragma once

constexpr int CELL_SIZE = 16;

// To change screen size.
constexpr float SCREEN_RESIZE = 2;

// To add offset to grid as per board size.
constexpr float OFFSET_HEIGHT = 3.37f;
constexpr float OFFSET_WIDTH = 0.75f;

// Board size.
constexpr int BOARD_HEIGHT = 415 / 2;  
constexpr int BOARD_WIDTH = 335 / 2;   

constexpr int FONT_HEIGHT = 16;
constexpr int FONT_WIDTH = 8;

// Grid size
constexpr int COLUMNS = 9;
constexpr int ROWS = 9;

constexpr int FAMRE_DURATION = 16667;
constexpr int EFFECT_DURATION = 16;

constexpr int MINES = 8;

